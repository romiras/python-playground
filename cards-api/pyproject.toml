[project]
name = "py-cards-api"
version = "0.1.0"
description = "Playing with decks of cards"
readme = "README.md"
requires-python = ">=3.12.4"
dependencies = [
    "pydantic>=2.12.5",
    "requests>=2.32.5",
]
